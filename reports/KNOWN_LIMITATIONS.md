# Known Limitations

- No 3D source asset or license evidence has been supplied.
- No Airbnb-owned assets or written rights to use them have been supplied; the selected client profile is planning-only.
- Blender is unavailable, so scene inspection and rendering cannot run.
- FFmpeg and FFprobe are unavailable, so video assembly and metadata validation cannot run.
- pnpm is unavailable, so website dependencies cannot be installed or built.
- The portfolio website has a passing build and lint check, but browser visual verification remains required because the in-app browser cannot access the local preview port in this environment.
- The contact form is intentionally disabled until an owned, configured contact destination is provided.
