# Pass 02 Verification

- `npm.cmd run build` passed in `website/`.
- `npm.cmd run lint` passed in `website/`.
- Local preview started successfully at `http://localhost:3000`.
- In-app browser verification was attempted at the local and reported network addresses but the browser service is network-isolated and returned connection refused.
